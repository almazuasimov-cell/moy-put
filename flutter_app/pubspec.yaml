name: moy_put
description: "Мой путь — AI Voice Diary"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  record: ^5.2.1
  shared_preferences: ^2.3.0
  fl_chart: ^0.69.0
  go_router: ^14.6.0
  permission_handler: ^11.3.0
  path_provider: ^2.1.0
  intl: ^0.19.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

dependency_overrides:
  record_linux: ^1.0.0

flutter:
  uses-material-design: true